<UserControl
    x:Class="AITool.Desktop.Views.SystemSettingsView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:AITool.Desktop.ViewModels"
    x:DataType="vm:SystemSettingsViewModel"
    x:CompileBindings="False">
  <Grid RowDefinitions="Auto,*" RowSpacing="24">
    <StackPanel Spacing="4">
      <TextBlock FontSize="22" FontWeight="Bold" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="系统设置" />
      <TextBlock Classes="page-subtitle" Text="配置检测、代理、日志和 OAuth 账号额度参数" />
    </StackPanel>
    <ScrollViewer Grid.Row="1"><StackPanel Spacing="14">
      <Border Background="{DynamicResource AppSuccessSoftBrush}" CornerRadius="8" Padding="12" IsVisible="{Binding HasMessage}"><TextBlock Foreground="{DynamicResource AppSuccessTextBrush}" Text="{Binding Message}" /></Border>
      <Border Background="{DynamicResource AppDangerSoftBrush}" CornerRadius="8" Padding="12" IsVisible="{Binding HasError}"><TextBlock Foreground="{DynamicResource AppDangerTextBrush}" Text="{Binding ErrorMessage}" TextWrapping="Wrap" /></Border>
      <Border Classes="content-card" Padding="18"><StackPanel Spacing="14"><TextBlock FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="检测设置" /><Grid ColumnDefinitions="*,*,*" ColumnSpacing="14"><StackPanel Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="检测超时（秒）" /><NumericUpDown Classes="form-input" Minimum="1" Value="{Binding Settings.DetectionRequestTimeoutSeconds}" /></StackPanel><StackPanel Grid.Column="1" Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="检测重试次数" /><NumericUpDown Classes="form-input" Minimum="0" Value="{Binding Settings.DetectionRetryCount}" /></StackPanel><StackPanel Grid.Column="2" Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="检测并发数" /><NumericUpDown Classes="form-input" Minimum="1" Value="{Binding Settings.DetectionConcurrency}" /></StackPanel></Grid></StackPanel></Border>
      <Border Classes="content-card" Padding="18"><StackPanel Spacing="14"><TextBlock FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="代理和熔断设置" /><Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="14"><StackPanel Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="代理超时（秒）" /><NumericUpDown Classes="form-input" Minimum="1" Value="{Binding Settings.ProxyRequestTimeoutSeconds}" /></StackPanel><StackPanel Grid.Column="1" Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="代理重试次数" /><NumericUpDown Classes="form-input" Minimum="0" Value="{Binding Settings.ProxyRetryCount}" /></StackPanel><StackPanel Grid.Column="2" Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="熔断失败阈值" /><NumericUpDown Classes="form-input" Minimum="1" Value="{Binding Settings.CircuitBreakerFailureThreshold}" /></StackPanel><StackPanel Grid.Column="3" Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="恢复时间（分钟）" /><NumericUpDown Classes="form-input" Minimum="1" Value="{Binding Settings.CircuitBreakerRecoveryMinutes}" /></StackPanel></Grid><Grid ColumnDefinitions="1.5*,1*" ColumnSpacing="14"><StackPanel Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="并发打满策略" /><ComboBox SelectedIndex="{Binding Settings.ConcurrencyMode}"><ComboBoxItem Content="跳过并尝试下一顺位模型" /><ComboBoxItem Content="排队等待并发槽位" /></ComboBox></StackPanel><StackPanel Grid.Column="1" Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="排队等待超时（秒）" /><NumericUpDown Classes="form-input" Minimum="1" Value="{Binding Settings.ConcurrencyQueueTimeoutSeconds}" /></StackPanel></Grid></StackPanel></Border>
      <Border Classes="content-card" Padding="18"><StackPanel Spacing="14"><TextBlock FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}" Text="日志和功能开关" /><Grid ColumnDefinitions="*,*,*" ColumnSpacing="14"><StackPanel Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="日志保留天数" /><NumericUpDown Classes="form-input" Minimum="1" Value="{Binding Settings.UsageLogRetentionDays}" /></StackPanel><CheckBox Grid.Column="1" Content="启用日志自动清理" IsChecked="{Binding Settings.UsageLogAutoCleanupEnabled}" VerticalAlignment="Bottom" /><CheckBox Grid.Column="2" Content="启用开发者功能" IsChecked="{Binding Settings.DeveloperFeaturesEnabled}" VerticalAlignment="Bottom" /></Grid><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="{Binding Settings.LastUsageLogPrunedText}" /><Grid ColumnDefinitions="*,*,*" ColumnSpacing="14"><CheckBox Content="启用 OAuth 账号功能" IsChecked="{Binding Settings.OAuthFeaturesEnabled}" /><CheckBox Grid.Column="1" Content="启用账号自动巡检" IsChecked="{Binding Settings.OAuthInspectionEnabled}" IsEnabled="{Binding Settings.OAuthFeaturesEnabled}" /><StackPanel Grid.Column="2" Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="巡检周期（秒）" /><NumericUpDown Classes="form-input" Minimum="30" Value="{Binding Settings.OAuthInspectionIntervalSeconds}" IsEnabled="{Binding Settings.OAuthFeaturesEnabled}" /></StackPanel></Grid><Grid ColumnDefinitions="*,*" ColumnSpacing="14"><StackPanel Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="额度缓存最长时间（小时）" /><NumericUpDown Classes="form-input" Minimum="1" Value="{Binding Settings.OAuthQuotaMaxCacheHours}" IsEnabled="{Binding Settings.OAuthFeaturesEnabled}" /></StackPanel><StackPanel Grid.Column="1" Spacing="6"><TextBlock FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}" Text="自动禁用阈值（%）" /><NumericUpDown Classes="form-input" Minimum="1" Maximum="100" Value="{Binding Settings.OAuthAutoDisableThresholdPercent}" IsEnabled="{Binding Settings.OAuthFeaturesEnabled}" /></StackPanel></Grid></StackPanel></Border>
      <StackPanel Orientation="Horizontal" Spacing="10"><Button Classes="primary-button" Command="{Binding SaveCommand}" Content="保存设置" IsEnabled="{Binding CanSave}" Padding="14,8" /></StackPanel>
      <Border Background="{DynamicResource AppResponseBackgroundBrush}" BorderBrush="{DynamicResource AppWarningSoftBrush}" BorderThickness="1" CornerRadius="8" Padding="18"><StackPanel Spacing="12"><TextBlock FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource AppWarningTextBrush}" Text="危险操作：清理调用日志" /><Grid ColumnDefinitions="*,*,*" ColumnSpacing="12"><ComboBox ItemsSource="{Binding ClearSourceOptions}" SelectedValue="{Binding ClearSource}" SelectedValueBinding="{Binding Value}"><ComboBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding Label}" /></DataTemplate></ComboBox.ItemTemplate></ComboBox><TextBox Grid.Column="1" Classes="form-input" Text="{Binding ClearStartTime}" Watermark="开始时间，例如 2026-08-01 00:00" /><TextBox Grid.Column="2" Classes="form-input" Text="{Binding ClearEndTime}" Watermark="结束时间，例如 2026-08-05 00:00" /></Grid><TextBlock FontSize="12" Foreground="{DynamicResource AppWarningTextBrush}" Text="{Binding ClearScopeText}" TextWrapping="Wrap" /><TextBlock FontSize="11" Foreground="{DynamicResource AppWarningTextBrush}" Text="时间按本地时间输入，提交时转换为 UTC；结束时间不包含该时刻。" TextWrapping="Wrap" /><StackPanel Orientation="Horizontal" Spacing="10"><Button Classes="secondary-button" Click="ConfirmClearFilteredLogs" Content="清空筛选范围" IsEnabled="{Binding CanClearLogs}" Padding="12,8" /><Button Classes="secondary-button" Click="ConfirmClearAllLogs" Content="清空全部日志" IsEnabled="{Binding CanClearLogs}" Padding="12,8" /></StackPanel></StackPanel></Border>
    </StackPanel></ScrollViewer>
    <Border Grid.Row="1" Background="{DynamicResource AppInfoSoftBrush}" CornerRadius="10" Padding="18" IsVisible="{Binding IsLoading}" ZIndex="10">
      <StackPanel Spacing="8" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,30,0,0">
        <ProgressBar Width="220" Height="5" IsIndeterminate="True" />
        <TextBlock Foreground="{DynamicResource AppInfoTextBrush}" Text="正在加载系统设置..." />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
